const config = {
    extend: {
        animation: {
            'floating': 'floating 3s ease-in-out infinite',
            'floating-2': 'floating 3s 0.5s ease-in-out infinite',
            'floating-3': 'floating 3s 1s ease-in-out infinite',
        }
    }
}

export default config;
